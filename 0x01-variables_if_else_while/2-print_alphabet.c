#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 *
 * Return: always return 0
 *
 */

int main(void)
{
	;

	 srand(time(0));
	 n = rand() - RAND_MAX / 2;
	 /* your code goes there */
	 return (0);
}
